apiVersion: v1
kind: Service
metadata:
  name: {{ include "homeinfra.fullname" . }}
  labels:
    {{- include "homeinfra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homeinfra.selectorLabels" . | nindent 4 }}
