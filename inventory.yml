all:
  children:
    nas:
      hosts:
        brikblok:

    k8s:
      children:
        k8s_control_plane:
          hosts:
            control1.cluster.wust.us:

        k8s_data_plane:
          hosts:
            worker1.cluster.wust.us:
            worker2.cluster.wust.us:
